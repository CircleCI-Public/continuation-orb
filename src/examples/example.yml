description: >
  Example configuration

usage:
  version: 2.1
  orbs:
    continuation: sandbox/continuation@1.2.3
  workflows:
    use-my-orb:
      jobs:
        - continuation/continue:
            configuration_path: "./config.yml"
